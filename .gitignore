# Dependencias instaladas por npm (regenerables con `npm install`)
# package-lock.json SÍ se commitea para reproducibilidad
node_modules/

# Bundle viejo (concatenación pre-esbuild) — ya no se usa
bundle.jsx

# Sourcemaps de build --dev — artefacto local, nunca al repo de produccion
*.map

# Config local del webhook — nunca al repo (contiene la URL activa; en local suele
# ser la de PRUEBA). Plantilla: env.example.js
env.js
