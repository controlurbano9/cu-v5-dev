{
  "name": "cu-v6",
  "version": "1.0.0",
  "private": true,
  "description": "Control Urbano V6 — bundle con esbuild",
  "scripts": {
    "build": "node build.js",
    "test": "node --test tests/utils.test.js",
    "lint": "eslint ."
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "eslint": "^10.8.1",
    "eslint-plugin-react-hooks": "^7.1.1"
  }
}
